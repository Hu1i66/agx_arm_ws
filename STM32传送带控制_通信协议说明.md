# STM32 传送带控制系统 —— 串口通信协议说明（ROS2 开发用）

> 适用工程：`D:\stm32\stm32揭榜挂帅，传送带控制`
> 主控：STM32F103C8T6（蓝丸最小系统板，72MHz，标准外设库 V3.5）
> 文档用途：为在 Ubuntu 中编写 ROS2 控制节点提供完整的通信协议与解析依据
> 文档依据：`User/main.c`（协议全部定义于此）

---

## 1. 系统作用概述

这是一个**传送带电机控制下位机程序**，通过串口与上位机（PC / ROS2 节点）交互，提供三类能力：

| 能力 | 说明 |
|---|---|
| **电机控制** | 通过 2 路继电器控制传送带电机：启停 + 正反转（开关量控制，无 PWM 调速） |
| **线速度测量** | TIM2 外部脉冲计数 + 轮径换算，实时测出传送带线速度（m/s） |
| **物体检测** | 红外传感器检测传送带上有无物料 |

**数据流向**：
- 上位机 → 下位机：单字节 ASCII 命令（`S` / `P` / `F` / `R`），用于控制电机
- 下位机 → 上位机：固定格式 ASCII 帧（`V:..,O:..,M1:..,M2:..`），每 100ms 上报一次
- 下位机 → 上位机：命令应答（`OK` / `ERR` / `BUSY`）

---

## 2. 串口通信参数（USART1）

| 参数 | 值 |
|---|---|
| 波特率 | **115200** |
| 数据位 | 8 位 |
| 停止位 | 1 位 |
| 校验位 | 无 |
| 流控 | 无 |
| 电平 | **3.3V TTL**（需 USB-TTL 转换器，注意：不是 RS232 电平） |
| 引脚 | PA9 = TX，PA10 = RX |

**Ubuntu 端串口打开参数**（示例）：
```bash
# 查看设备
ls /dev/ttyUSB* /dev/ttyACM*
# 添加 dialout 组权限（一次性）
sudo usermod -aG dialout $USER
# stty 参数参考
stty -F /dev/ttyUSB0 115200 cs8 -cstopb -parenb -ixon raw
```

---

## 3. 指令协议（上位机 → 下位机）

### 3.1 命令表（`Process_Cmd()`，`main.c` 第 129 行）

| 命令字符 | ASCII | 功能 | 执行动作 | 应答 |
|---|---|---|---|---|
| `S` | 0x53 | 启动 | RELAY1 吸合，电机通电运转 | `OK` |
| `P` | 0x50 | 停止 | RELAY1 释放，电机断电停止 | `OK` |
| `F` | 0x46 | 正转 | 先停止 → 延时 500ms → RELAY2 吸合（正转）→ **自动重新启动** | `OK`（延时结束后才回复） |
| `R` | 0x52 | 反转 | 先停止 → 延时 500ms → RELAY2 释放（反转）→ **自动重新启动** | `OK`（延时结束后才回复） |
| 其他任意字节 | — | 非法命令 | 无动作 | `ERR` |
| （换向等待期间任意命令） | — | 忙状态 | 拒绝执行 | `BUSY` |

### 3.2 重要时序约束

1. **一次发一个字节**。程序按字节接收（RXNE 中断），多发不报错，但回车换行（`0x0D`/`0x0A`）会被当作非法命令返回 `ERR`。建议只发命令字符本身。
2. **必须大写**。`switch(cmd)` 精确匹配 `'S'/'P'/'F'/'R'`，小写返回 `ERR`。
3. **换向流程是"停 → 等 → 换向 → 自动启动"**：`F`/`R` 发出后，下位机先停电机，等待 **500ms**（`MOTOR_STOP_DELAY`）后才切换方向并自动重启电机，此时才回复 `OK`。期间（`restart_pending=1`）收到的任何命令都会返回 `BUSY`。
4. **建议的指令串行化**：ROS2 节点发送命令后应等待应答 `OK` 再发下一条；收到 `BUSY` 表示下位机正在换向等待，应稍后重试（或直接等待，因为 500ms 后流程自动完成）。

### 3.3 应答帧

| 应答 | 含义 |
|---|---|
| `OK\r\n` | 命令执行成功（`F`/`R` 为换向完成、电机已重启后回复） |
| `ERR\r\n` | 非法命令 |
| `BUSY\r\n` | 正在换向等待（500ms 内），拒绝新命令 |

---

## 4. 状态上报协议（下位机 → 上位机）

### 4.1 帧格式

```
V:2.04,O:1,M1:1,M2:1\r\n
```

`main.c` 第 104 行：`sprintf(send_buf, "V:%.2f,O:%d,M1:%d,M2:%d\r\n", ...)`

- 周期：**每 100ms** 一帧（与测速采样同步）
- 帧结束符：`\r\n`
- 字段分隔：`,`；字段名：`V` / `O` / `M1` / `M2` + 冒号 `:` + 值

### 4.2 字段含义

| 字段 | 含义 | 类型 | 取值范围 | 说明 |
|---|---|---|---|---|
| `V` | 传送带线速度 | float | ≥ 0（两位小数） | 单位 **m/s**，见 5.2 换算公式 |
| `O` | 物体检测标志 | int | 0 / 1 | **1 = 检测到物体**（PA0 读到低电平）；0 = 无物体 |
| `M1` | 继电器1（启停）状态 | int | 0 / 1 | 1 = 电机运行；0 = 电机停止 |
| `M2` | 继电器2（方向）状态 | int | 0 / 1 | 1 = 正转；0 = 反转（上电默认 0） |

### 4.3 解析建议（ROS2）

- 以 `\r\n` 为帧边界进行缓冲分帧；
- 用正则或拆分解析：`V:([\d.]+),O:(\d),M1:(\d),M2:(\d)`；
- 数值校验：V 必须 ≥ 0，O/M1/M2 必须为 0 或 1，字段不完整或超时丢弃该帧。

---

## 5. 关键参数与换算（测速部分）

`main.c` 第 5-10 行定义：

| 宏 | 值 | 含义 |
|---|---|---|
| `WHEEL_DIAMETER` | 0.05 m | 测速轮直径 50mm |
| `PULSE_PER_ROUND` | 20 | 测速传感器每转输出 20 个脉冲 |
| `SAMPLE_PERIOD_MS` | 100 | 采样/上报周期 100ms |

**换算公式**（`main.c` 第 75 行）：
```
STEP_DISTANCE = π × WHEEL_DIAMETER / PULSE_PER_ROUND = π×0.05/20 ≈ 0.007854 m/脉冲
V (m/s) = 100ms 内脉冲数 × STEP_DISTANCE / 0.1s
```

> 注意：若实际传感器的每转脉冲数不是 20，或测速轮直径不是 50mm，速度值会系统性偏差，需修改对应宏并重新编译。

---

## 6. 状态机逻辑（下位机侧）

下位机主循环构成一个三态模型：

```
[空闲] --收到 S/P--> [执行并回复 OK] --> [空闲]
[空闲] --收到 F/R--> [停止电机] --> [换向等待 500ms(restart_pending=1)] 
                     --> [切换方向 + 自动启动] --> [回复 OK] --> [空闲]
[换向等待中] --收到任意命令--> [回复 BUSY]（不改变当前流程）
```

- 换向等待由主循环 100ms 节拍累计实现（非阻塞，`restart_timer` 每 100ms +1，满 500ms 执行）。
- 注意：**`F`/`R` 会自启动电机**，即换向完成后无需再发 `S`。

---

## 7. 硬件接线速查（如需对照）

| 外设 | 引脚 | 配置 | 信号含义 |
|---|---|---|---|
| 红外传感器（物体检测） | **PA0** | 上拉输入 | 低电平 = 有物体（O:1） |
| 测速传感器（编码器/霍尔） | **PA1** | 上拉输入 + TIM2_CH2 外部计数 | 上升沿计数，每转 20 脉冲 |
| 继电器1（启停） | **PB6** | 推挽输出 | 高 = 吸合 = 电机运行 |
| 继电器2（正反转） | **PB7** | 推挽输出 | 高 = 正转，低 = 反转 |
| USART1 TX | **PA9** | 复用推挽 | 接 USB-TTL RX |
| USART1 RX | **PA10** | 浮空输入 | 接 USB-TTL TX |
| 继电器模块供电 | DC1/DC2 | — | DC1 → 5V+，DC2 → GND（共地） |
| 传感器供电 | VCC/GND | — | 3.3V 或 5V（按传感器标称，共地） |

---

## 8. ROS2 节点设计要点（建议）

1. **串口层**：115200-8N1，raw 模式；建议使用 `pyserial`（Python）或 `serial`（C++）驱动；帧解析线程与命令发送分离。
2. **接口映射建议**（可按项目需求调整）：
   - 速度 → `std_msgs/Float32`（发布，频率 10Hz，与上报周期一致）
   - 物体检测 → `std_msgs/Bool`（发布）
   - 启停/方向 → `std_srvs/Trigger` 或自定义服务（服务端串行发送命令并等待应答）
   - 全部状态 → `sensor_msgs/JointState` 或自定义消息（含 V/O/M1/M2 四字段）
3. **应答-超时机制**：发送 `S/P` 后 100ms 内未收到 `OK` 判定超时；发送 `F/R` 后至少等待 **600ms** 收 `OK`（500ms 换向 + 余量）。
4. **`BUSY` 处理**：收到 `BUSY` 表示下位机在换向等待，节点应等待 500ms 后重试，不要连续轰炸。
5. **权限**：串口设备需 `dialout` 组权限；ROS2 launch 中可用 `param_command` 或 `udev` 规则固定 `/dev/ttyUSB0` 设备名。

---

*文档生成时间：2026-08-10。以上协议内容均来源于工程源码 `User/main.c`，如有源码变更请同步更新本表。*
