# AGX 机械臂智能分拣系统 — 项目进度报告

**报告日期**: 2026 年 7 月 27 日  
**报告人**: 陆祥飞  
**参赛项目**: AGX 机械臂自动分拣系统

---

## 一、项目整体架构概述

本项目面向智能仓储分拣场景，构建了一套基于 ROS2 的机械臂自动分拣系统。系统由 **视觉感知**、**运动规划** 和 **人机交互** 三大子系统组成，各模块协同工作，实现了从物体识别到精准抓取投放的全自动流程。

### 1.1 系统总体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                        GUI 控制面板                                │
│              (sorting_gui_client.py - Tkinter)                     │
│         手动抓取 / 自动分拣 / 排队任务 / 状态监控                    │
└──────────────────────────┬───────────────────────────────────────┘
                           │ cmd_queue (ROS topic)
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                    抓取动作执行核心                                 │
│              (auto_sorting_action.py - MoveIt2)                    │
│         路径规划 / IK求解 / 碰撞检测 / 夹爪控制 / 料框投放           │
└───┬──────────────────────┬────────────────────────────────────────┘
    │                      │
    ▼                      ▼
┌───────────────┐   ┌──────────────────────────────────┐
│  视觉感知层    │   │         硬件驱动层                 │
│               │   │                                  │
│ • YOLO 检测   │   │  • CAN总线 (机械臂关节控制)        │
│   (水果/物品) │   │  • 夹爪 (AgxGripper)              │
│ • 蓝方块 HSV  │   │  • 传送带 (CH340 继电器)          │
│   检测       │   │  • MQTT 状态广播                  │
│ • EMA 滤波   │   │                                  │
│   多目标追踪  │   │                                  │
│ • D455 深度  │   │                                  │
│ • GraspNet   │   │                                  │
│   6DoF 位姿  │   │                                  │
└──────┬────────┘   └──────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│              物理设备层                            │
│  D455 深度相机 | Piper 6轴机械臂 | 末端夹爪         │
│  传送带 | 两个分拣料框                            │
└──────────────────────────────────────────────────┘
```

### 1.2 各模块职责与相互关系

| 模块 | 核心文件 | 职责 |
|------|---------|------|
| **视觉感知** | `realsense_yolo_node.py`、`blue_block_detector.py`、`graspnet_service_node.py` | 识别传送带上的物体（位置、类别、尺寸），计算在机械臂坐标系下的 3D 坐标 |
| **运动规划** | `auto_sorting_action.py` | 接收抓取目标，使用 MoveIt2 规划无碰撞路径，通过 CAN 总线驱动机械臂执行 |
| **GUI 交互** | `sorting_gui_client.py` | 提供操作界面：实时相机画面、手动/自动分拣按钮、料框选择、状态显示 |
| **硬件驱动** | CAN 接口 + CH340 继电器 | 控制机械臂关节运动、夹爪开合、传送带启停 |
| **通信桥接** | `robot_status_bridge.py` | 通过 MQTT 协议向外部系统（赛事 B 组后端）上报分拣结果 |

**数据流向**：相机拍照 → YOLO/HSV 检测物体 → 手眼标定转换为机械臂坐标 → MoveIt2 计算运动轨迹 → CAN 总线发送关节指令 → 机械臂执行抓取 → 夹爪闭合夹取 → 移动到指定料框放置 → 回到观测位。

### 1.3 两种检测模式

系统支持两种并行推进的视觉检测方案：

| 模式 | 适用场景 | 核心技术 |
|------|---------|---------|
| **水果 YOLO 检测** | 苹果、橙子、草莓、柠檬等不规则物体 | YOLOv8 深度学习 + D455 深度相机 + EMA 多目标追踪滤波 |
| **蓝方块 HSV 检测** | 规则长方体物块（技术验证） | HSV 颜色分割 + RANSAC 平面拟合 + PCA 主轴朝向分析 |

蓝方块模式作为技术积累平台，其上验证成功的 PCA 稳定朝向、动态 Z 偏移等技术，后续将迁移回水果分拣系统。

---

## 二、当前已完成工作与进展

### 2.1 系统基础建设（已完成）

- **机械臂底层驱动**：CAN 总线通信（1Mbps）、关节角度/力矩反馈、夹爪力控（0.5-3.0N）
- **MoveIt2 运动规划**：避碰路径生成、逆运动学（IK）求解、关节空间/笛卡尔空间混合规划
- **D455 深度相机集成**：三级后处理滤波链（空间滤波 + 时间滤波 + 空洞填充）、手动曝光 1600、自标定
- **手眼标定**：相机坐标系到机械臂基坐标系的精确映射（eye-to-hand 配置，相机固定安装）
- **Gazebo 仿真环境**：传送带、料框、机械臂完整的仿真场景

### 2.2 视觉检测与追踪（已完成）

- **YOLOv8 水果检测**：支持苹果、橙子、草莓、柠檬、桃、梨等多类物体识别，实时输出边界框和类别
- **EMA 滤波多目标追踪**：通过指数移动平均（α=0.25）消除坐标抖动，深度测量波动从 ±0.02m 降至 ±0.004m，支持 10 帧内目标丢失自动续接
- **蓝方块检测节点**：独立 HSV 颜色分割 + RANSAC 平面拟合深度估计 + PCA 主轴姿态稳定
- **D455 双目深度融合**：订阅 `camera_info` 获取精确内参，替代早期硬编码值，深度精度大幅提升

### 2.3 自动分拣功能（已完成）

- **状态机驱动的自动分拣**：IDLE → CHECK_LEFT（检测画面左侧物体）→ SORTING（分拣）→ 循环，支持随时停止并安全回观测位
- **智能传送带控制**：黄色分界线（位于画面中央）判断物体是否到达抓取区域，自动启停传送带
- **分箱规则**：苹果、草莓、橙子 → 料框 1；柠檬、桃、梨 → 料框 2
- **失败重试机制**：抓取失败自动重试一次，仍失败则跳过该物体，避免系统卡死
- **GUI 完整交互**：启停按钮、状态标签、手动/自动模式切换

### 2.4 抓取精度优化（已完成）

| 优化项 | 效果 |
|--------|------|
| **动态 Z 偏移** | 通过正运动学（FK）实测几何关系，推导 `手指底部恒定高于台面 4mm` 的约束，使 3cm/4cm/5cm 不同高度物块均能精准夹取 |
| **PCA 主轴姿态** | 对物体点云做协方差特征分解，获取稳定主轴角度，消除立方体角度跳变（原 minAreaRect 在正方形上跳变 90°） |
| **joint6 预规划** | 移动到抓取点上方时即完成末端姿态对齐，下降过程中关节 6 保持不转，避免夹爪碰撞 |
| **分步间连续运动** | 观测位→料框上方→放置位的途径点改为连续轨迹，消除中间停顿，单次抓取周期从 17s 降至约 10s |
| **夹爪力控优化** | 闭合目标简化为 `物体直径 - 0.020m`（最小闭合 0.020m），移除冗余的二次压力补偿 |

### 2.5 安全与通信（已完成）

- **碰撞检测管理**：在抓取/投放阶段自动临时禁用传送带相关碰撞体（12 个碰撞模型），关键运动完成后恢复，兼顾安全与规划成功率
- **急停功能**：支持随时紧急停止所有机械臂运动
- **MQTT 状态广播**：每个分拣周期结束后通过 MQTT 向外部系统上报完整的抓取数据（物体类别、坐标、成功/失败状态等）
- **GUI 进程安全**：将所有 subprocess 调用迁移至独立子进程，彻底解决 Tkinter + fork() 导致的段错误崩溃

### 2.6 当前工作日志摘要（2026-07-27）

| 事项 | 状态 |
|------|------|
| D455 深度滤波链 + 手动曝光优化 | 已完成 |
| 蓝方块 HSV 检测 + minAreaRect 开发 | 已完成 |
| 动态 Z 偏移（不同高度物块自适应） | 已完成 |
| PCA 角度替代 minAreaRect（立方体稳定） | 已完成 |
| 夹爪闭合后力矩检测（抓取成功判断） | 设计中 |
| 30 次静态抓取成功率统计 | 待测试 |
| 不同光照下 HSV 阈值鲁棒性验证 | 待测试 |

---

## 三、传送带物体随动抓取 — 后续开发计划

### 3.1 功能目标

实现机械臂在传送带持续运动状态下，对移动中的物体进行实时追踪并动态抓取。当前系统为"传送带停→抓取→传送带开"模式，随动抓取升级后传送带无需启停，机械臂跟踪物体运动轨迹，在运动中完成抓取，大幅提升分拣效率。

### 3.2 关键技术点

| 技术点 | 说明 |
|--------|------|
| **运动物体状态估计** | 利用 EMA 追踪获取物体的实时位置与速度（已有追踪器，需增加速度计算） |
| **传送带运动模型** | 标定传送带运动速度与方向，建立物体运动的预测模型（匀速直线运动） |
| **拦截点规划** | 根据机械臂运动能力和物体预测轨迹，计算最优抓取拦截位置和时间 |
| **动态路径规划** | 机械臂在运动过程中实时更新目标位姿，规划赶到拦截点的最优轨迹 |
| **时间同步** | 相机帧、物体位置估计、机械臂运动的精确时间对齐 |
| **安全保护** | 动态场景下的碰撞检测（运动中的物体可能在任意时刻进入机械臂工作空间） |

### 3.3 实施步骤与阶段划分

#### 阶段一：运动物体感知与状态估计（预计 1-2 周）

| 任务 | 内容 |
|------|------|
| 1.1 传送带速度标定 | 放置已知尺寸的标定物，测量其在像素空间和物理空间中的移动速度，建立传送带匀速运动模型 |
| 1.2 物体速度估计 | 在现有 EMA 追踪器中增加速度分量（Δt 帧间位移/时间差），输出物体实时运动矢量 |
| 1.3 时间戳对齐 | 为每帧检测结果打上精确的系统时间戳，确保物体位置的时间有效性 |

**验收标准**：追踪器输出物体位置 + 速度矢量，传送带速度标定误差 < 5%

#### 阶段二：拦截点计算与路径规划（预计 2-3 周）

| 任务 | 内容 |
|------|------|
| 2.1 拦截点求解 | 根据物体当前位置、速度、机械臂从观测位到抓取区域的最短运动时间，计算最优拦截坐标 |
| 2.2 预到位策略 | 机械臂提前移动到传送带旁预设"待命位"，缩短赶到拦截点的距离和时间 |
| 2.3 动态目标跟随 | 使用 MoveIt2 的 `planning_scene` 动态更新目标帧，规划追上移动物体的连续轨迹 |
| 2.4 速度匹配下降 | 机械臂末端 xy 速度与物体运动同步，z 轴正常下降，实现运动中夹取 |

**验收标准**：仿真环境中机械臂能追上以 0.05m/s 匀速运动的目标并成功夹取

#### 阶段三：系统集成与实机测试（预计 2-3 周）

| 任务 | 内容 |
|------|------|
| 3.1 实机联调 | 将所有模块集成到实机环境，验证从检测到运动中抓取的完整链路 |
| 3.2 参数调优 | 调整拦截提前量、待命位置、运动速度限制等参数，优化抓取成功率和稳定性 |
| 3.3 失败处理 | 实现追踪丢失的恢复策略、拦截失败的回退方案 |
| 3.4 性能测试 | 100 次连续随动抓取测试，统计成功率、平均周期时间 |

**验收标准**：实机随动抓取成功率 ≥ 90%，单周期时间 ≤ 当前静态抓取的 1.5 倍

#### 阶段四：性能优化与鲁棒性提升（预计 1-2 周）

| 任务 | 内容 |
|------|------|
| 4.1 多种速度适配 | 测试不同传送带速度（0.02-0.10m/s），自适应调整拦截策略 |
| 4.2 多物体队列 | 连续多物体依次随动抓取，避免物体堆积和漏抓 |
| 4.3 异常恢复 | 抓取失败、物体跑出工作空间等异常情况的安全处理 |
| 4.4 最终测试与文档 | 500 次综合测试，撰写操作手册和技术文档 |

**验收标准**：连续运行 30 分钟无系统崩溃，综合抓取成功率 ≥ 85%

### 3.4 预期时间线

```
阶段一: 运动感知   ████████░░░░░░░░░░░░░░  1-2 周
阶段二: 拦截规划   ░░░░░░░░████████████░░░  2-3 周
阶段三: 实机集成   ░░░░░░░░░░░░░░░░████████  2-3 周
阶段四: 优化测试   ░░░░░░░░░░░░░░░░░░░░████  1-2 周
                  ─────────────────────────────
                  总计预计:               6-10 周
```

### 3.5 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| 相机帧率不足导致位置滞后 | 拦截失败 | 检查 D455 帧率优化，必要时降低分辨率换取帧率 |
| 机械臂运动速度跟不上传送带 | 物体跑出工作空间 | 在阶段一标定时就确定传送带最高可处理速度，限制在安全范围 |
| 运动中的碰撞检测误报 | 规划失败 | 扩大运动中碰撞检测的容许范围，或在特定阶段临时放宽 |
| 实机与仿真行为不一致 | 调试周期延长 | 尽早启动实机联调（阶段二中期即开始并行验证） |

---

## 四、总结

本项目已完成了机械臂自动分拣系统的核心功能建设，包括视觉检测、运动规划、自动分拣全流程。当前静态分拣已具备稳定运行能力，后续重点是将蓝方块验证的技术（PCA 姿态对齐、动态 Z 偏移）迁移回水果分拣，并在此基础上攻克传送带随动抓取这一核心技术难点，实现从"停→抓→走"到"运动中抓取"的效率跃升。
